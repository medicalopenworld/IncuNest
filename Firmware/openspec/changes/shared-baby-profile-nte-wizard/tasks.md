## 1. Shared — NTE calculation engine (commit scope: `shared`)

- [x] 1.1 Spike: confirm PlatformIO's Library Dependency Finder picks up a new `shared/src/*.cpp` from `motherBoard` and `Display_HMI` via their existing `lib_extra_dirs = ../shared` without further config (design decision 2). If not, add `"build": {"srcDir": "src"}` to `shared/library.json`.
  - **Result, more nuanced than assumed**: the Arduino/hardware envs (`motherBoard`'s `IncuNest_V16/17/18`, `Display_HMI`'s `main`) pick up `shared/src/nte_table.cpp` via LDF automatically — confirmed with a temporary smoke-call file compiled and linked successfully on both. `motherBoard`'s `[env:native]` (SCons/native platform) does **not** — even after adding `"native"` to `shared/library.json`'s `platforms` and an explicit `"build": {"srcDir": "src"}`, `.pio/libdeps/native` never contained the shared lib. Root-caused to `lib_extra_dirs` treating `../shared` as a *directory of libraries* (its `include/` subfolder auto-registers as an anonymous header-only lib, which is why headers always worked) rather than `../shared` itself (where `library.json` actually lives) being recognized as one library — an existing latent gap, invisible until now because `shared/` was header-only. Fixed by explicitly compiling `../shared/src/*.cpp` via `env.BuildSources()` in `motherBoard/pre_native.py`, scoped to `[env:native]` only; both `library.json` changes are kept (harmless, and the `srcDir` hint documents intent even though native needed the extra step).
- [x] 1.2 Add `nte_table.h`/`nte_table.cpp` to `motherBoard`'s `[env:native]` `build_src_filter` and write failing Unity tests in `motherBoard/test/test_nte_table/test_nte_table.cpp` covering every scenario in `specs/nte-calculation-engine/spec.md` (deterministic output, known-weight bounded range, SKIP sentinel, out-of-table fail-safe).
  - Note: no `build_src_filter` entry was needed/possible (it only selects files under `src_dir`, not `../shared`) — the `pre_native.py` fix from 1.1 supersedes this. Tests written red-first (confirmed linker error before `nte_table.cpp` existed), 6 scenarios covering all 4 requirements.
- [x] 1.3 Implement `shared/include/nte_table.h` (`NteRange` struct, `calculateNteRange()` declaration) and `shared/src/nte_table.cpp`, porting the table/interpolation logic out of `Display_HMI/src/tasks/UITask.cpp:901-1060+` as a pure function.
- [x] 1.4 `pio test -e native` (motherBoard) passes for `test_nte_table`. — 6/6 pass; full `pio test -e native` (15 cases across `test_alarms`/`test_nte_table`/`test_pid`) also green, no regression.
- [x] 1.5 `pio run -e main` passes with no new warnings on **both** `motherBoard` and `Display_HMI` (confirms decision 2's LDF assumption in practice, even though neither board's production code calls the new function yet).
  - motherBoard has no `main` env (real envs are `IncuNest_V16/17/18`, per `.claude/rules` — `testing.md`'s `-e main` wording is generic); ran `pio run -e IncuNest_V18` instead, plus a temporary smoke-call file (removed after) to actually exercise the link step per 1.1's finding. Both boards: SUCCESS, no new warnings.

## 2. motherBoard — baby profile storage (commit scope: `motherboard`)

- [x] 2.1 Write failing Unity tests in `motherBoard/test/test_baby_profile/test_baby_profile.cpp` for the pure parts of `specs/baby-profile-storage/spec.md`: slot creation, `nextSeq` persistence/monotonicity, FIFO eviction by lowest `seq`, history-record encoding, circular-buffer cursor advance/wraparound at 1,000 records, and age-derivation from `admissionEpoch` (including the "no synced time available" branch). — Red confirmed (link errors) before implementing.
- [x] 2.2 Add the new test file(s) to `[env:native]`'s `build_src_filter` alongside the pure logic they cover. — Added `modules/baby_profile/baby_profile_core.cpp`; test sources are auto-collected by the test runner (one `main()` per test folder — weight tests moved to `test/test_weight_history/` for that reason).
- [x] 2.3 Implement `motherBoard/{include,src}/modules/baby_profile/` (per `.claude/rules/embedded-motherboard.md`'s module boundaries): `BabyProfile` struct, `NS_BABY` NVS access (3 slots + `nextSeq` key), FIFO eviction, age derivation from `admissionEpoch` + synced time. — Split as `baby_profile_core.{h,cpp}` (pure, native-tested) + `baby_profile_store.{h,cpp}` (NVS `mb_baby` namespace + LittleFS I/O, hardware-only). `nextSeq` persisted synchronously before `slotUsed` (decision 3).
- [x] 2.4 Implement the LittleFS circular history log (`/baby_history.log` on the LittleFS mount): 8-byte header (`writeIndex`, `count`) + 1,000 fixed-size 41-byte record slots (40-byte record + 1-byte `valid` flag, design decision 8), append-on-eviction, wraparound overwrite.
- [x] 2.5 Weight-evolution tests written red-first in `motherBoard/test/test_weight_history/test_weight_history.cpp` (own folder, not inside `test_baby_profile/` — two Unity `main()`s can't share a folder): dedup, circular cursor, budget accounting, unified-eviction decision for both trigger orders.
- [x] 2.6 Implement `weight_active/<seq>.bin` (append-on-`PROFILE_WEIGHT`, dedup check, 1,000-point circular cursor) and the move-to-`weight_archive/<seq>.bin` (LittleFS `rename`) inside the single `archiveProfile()` routine.
- [x] 2.7 Implement the 600 KB weight-archive byte-usage index (per-`seq` bytes, in-RAM), rebuilt from a directory scan of `weight_archive/` at every `babyStore_init()` (always-rebuild is simpler and strictly safer than trust-then-verify; the index is small).
- [x] 2.8 Implement the unified eviction trigger in `archiveProfile()` + `historyAppend()`: budget-first loop deletes oldest archives and tombstones their audit records (`valid=false`); audit-cap overwrite pair-deletes the outgoing record's archive file. History read path skips `valid=false`.
- [x] 2.9 `pio test -e native` passes for `test_baby_profile` and the new weight-history tests. — 45/45 across all 5 native test suites.
- [x] 2.10 `dischargeEpoch`/`outcome` fields in `BabyProfile` (NVS blob + 41-byte audit record), round-trip + default-Unknown tests in `test_baby_profile.cpp`.
- [x] 2.11 Single `archiveProfile(slotIdx)` routine (design decision 9) with two entry points: FIFO auto-evict (`babyStore_createProfile` when full) and explicit discharge (`babyStore_discharge`). FIFO path leaves `dischargeEpoch=0`/`outcome=Unknown` untouched. (Pure decision helpers unit-tested; the routine itself is LittleFS-bound, manual verification in 2.20.)
- [x] 2.12 Explicit discharge implemented: stamps `dischargeEpoch` (now-if-synced else 0)/`outcome`, archives immediately, frees the slot, clears `activeSeq` if it pointed at the discharged baby.
- [x] 2.13 Downsampling tests written red-first (in `test_weight_history.cpp` alongside the other weight tests rather than a separate `test_downsample.cpp` — same coverage: passthrough ≤50, evenly-spaced reduction with endpoints kept + strictly-increasing indices, determinism, zero-points edge).
- [x] 2.14 `baby_downsample_indices()` implemented in `baby_profile_core.cpp` per design decision 11 (integer rounding, no floats).
- [x] 2.15 `pio test -e native` passes for discharge/downsampling coverage (45/45 total).
- [x] 2.16 `activeSeq` FIFO-exclusion tests in `test_baby_profile.cpp` (skip-active-even-if-lowest, defensive reject when no eligible slot). Set/clear/default-0 is NVS-bound (store layer), covered by 2.20 manual checklist.
- [x] 2.17 `mb_baby/activeSeq` NVS key implemented in the store (`babyStore_get/setActiveSeq`, persisted, default 0). Wiring the set/clear calls into the existing AIR/SKIN control-on/off protocol handling happens in phase 3 (comm layer), where that handler lives.
- [x] 2.18 FIFO eviction excludes `seq == activeSeq` (`baby_pick_eviction_slot`); `babyStore_createProfile` returns 0 (refusal) when no eligible slot exists.
- [x] 2.19 `pio test -e native` passes for the `activeSeq`/FIFO-exclusion tests.
- [ ] 2.20 Manual verification on real hardware (document what was tested): 4th-profile FIFO eviction, history log survives a reboot, history log after a rotation past 1,000 records (can be checked with a reduced cap for the manual test, not by physically creating 1,000 profiles), reboot mid-wizard before `PROFILE_WEIGHT` leaves NVS untouched, weight chart populates for an active baby across multiple wizard runs with changing weights, an evicted baby's weight file is found intact under `weight_archive/` after eviction, the 600 KB archive budget is respected (can be checked with a reduced budget for practicality) and triggers eviction of the oldest archived baby before the audit log's own 1,000-record cap would, after any eviction the oldest baby's audit record and archived weight file are both gone (never just one), explicit discharge frees a slot immediately with fewer than 3 active profiles in use, a discharged baby's audit record shows the correct `dischargeEpoch`/`outcome`, activating AIR or SKIN for a given baby and then creating two more new profiles confirms that baby's slot is never evicted even when it holds the lowest `seq`, `activeSeq` survives a motherBoard reboot mid-treatment, and turning control off then creating a 4th profile confirms the previously-protected baby can now be evicted normally.

## 3. motherBoard — baby profile protocol (commit scope: `motherboard`)

- [x] 3.1 Protocol tests written red-first in `motherBoard/test/test_baby_protocol/test_baby_protocol.cpp` (parse: all 8 message types incl. SKIP + malformed discard for short field count/non-numeric/out-of-range outcome/empty name; build: list/empty list/range both branches/history page/weight history + bounded-buffer behavior). 21/21 pass.
- [x] 3.2 Added `modules/baby_profile/baby_profile_protocol.cpp` to `[env:native]`'s `build_src_filter`.
- [x] 3.3 Pure parse/build in `modules/baby_profile/baby_profile_protocol.{h,cpp}`; thin dispatch in `CommTask.cpp` (`handleBabyLine`) calling `babyStore_*` + `calculateNteRange` — no logic duplicated in the comm layer. **Implementation-revealed protocol addition**: `CTRL,PROFILE_ACK,<seq>` (response to NEW/SELECT/DISCHARGE) — the spec's message set had no way for the HMI to learn the new profile's `seq` after `PROFILE_NEW`; `seq=0` = refused/unknown. Documented in PROTOCOL.md.
- [x] 3.4 Legacy fields removed end-to-end on motherBoard: recurring-`HMI,` parse trimmed to 10 fields, `HMI_CommandMessage`/`Proto_HmiCommand` structs cleaned, `main.cpp` legacy log block removed. GPRS/Wifi_OTA telemetry re-sourced from `babyStore_telemetryProfile()` + one-shot `babyStore_telemetryDirty()` flag (same consume-once semantics as before), now also publishing `baby_name`/`baby_outcome`/`baby_discharge_epoch` (intentional, decision 14). Discharge snapshots the profile before the slot is freed so its final record still publishes.
- [x] 3.5 Covered by 3.1 (single protocol test suite; page-slice/tombstone logic itself is store+LittleFS-bound → 2.20 manual checklist).
- [x] 3.6 `PROFILE_DISCHARGE` wired to `babyStore_discharge()` (+ ACK).
- [x] 3.7 `PROFILE_HISTORY_REQ` wired to `babyStore_readHistoryPage()` (stateless, newest-first, skips tombstones).
- [x] 3.8 `WEIGHT_HISTORY_REQ` wired to `babyStore_readWeightHistory()` (active-first then archive lookup, ≤50 downsampled points).
- [x] 3.9 `PROTOCOL.md` bumped to v2.0.0: all new messages documented (incl. `PROFILE_ACK`), legacy 13-field drift corrected with an explicit breaking-change note, validation rules stated.
- [x] 3.10 `pio test -e native`: 66/66 across 6 suites.
- [x] 3.11 `pio run` green on `IncuNest_V18` **and** `IncuNest_V16` (no `main` env exists on motherBoard). Includes a build-system fix: `lib_extra_dirs = ../shared` never linked `shared/src/*.cpp` (it treats `shared/`'s subfolders as libraries — headers came from an anonymous "include" pseudo-lib); replaced with `lib_deps = symlink://../shared` so `library.json` is honored. `[env:native]` additionally keeps the `pre_native.py` explicit-compile fallback (symlink alone verified insufficient there).
  - **activeSeq wiring (from 2.17)**: done in `CommTask.cpp`'s recurring-`HMI,` handler — OFF→ON stamps the wizard-selected `seq`, ON→OFF clears.
- [ ] 3.12 Verify a real `HMI,WEIGHT_HISTORY_REQ` round-trip at the maximum 50-point response size fits the serial RX line buffer on both boards (design decision 11's open verification item); if it does not, fall back to a lower `maxOut` or chunked response before proceeding to phase 5.
- [ ] 3.13 Manual verification on real hardware with a provisioned GPRS and/or WiFi connection (document what was tested): the ThingsBoard telemetry payload for a pending baby-data change includes `name`, `outcome`, and `dischargeEpoch` alongside the existing `weightGrams`/`gestWeeks`/`ageDays` keys, on both the GPRS and WiFi publish paths (`GPRS.cpp`/`Wifi_OTA.cpp` are not covered by `[env:native]`).

## 4. Display_HMI — activation wizard (commit scope: `hmi`)

- [x] 4.1 Removed the AUTO AIR popup and its local NTE calculation from `UITask.cpp` (functions `autoair_*`/`aa_*`/`AutoAirBtn_cb`, ~800 lines) and its widget creation from `ElementsCreation.cpp` (`create_autoair_button`/`create_autoair_popup`, ~540 lines); removed `babyWeightGrams`/`babyGestWeeks`/`babyAgeDays` from `HMI_Message`/`Proto_HmiCommand` and the recurring `HMI,...` line builder in `CommTask.cpp`.
  - **Process note for future large deletions**: the AUTO AIR banner comment in `UITask.cpp` was not a clean contiguous block — two unrelated functions (`photo_safety_popup_show`/`photo_safety_overlay_cb`/`photo_turnon_cb`, `update_main_toggle_buttons`) and two more in `ElementsCreation.cpp` (`create_photo_safety_popup`, `create_main_toggle_buttons`) were interleaved inside the range and got deleted collaterally by an initial bulk-range removal. Caught via the first build (undefined references) and confirmed fully fixed by diffing the full function-name list against `git show HEAD:...` before/after — restored verbatim from git history, zero behavior change. Lesson: for a "delete lines N-M" this large, diff the function inventory against git HEAD afterward rather than trusting a banner-comment boundary.
- [x] 4.2 `PROFILE_*` protocol client added to `CommTask.h`/`.cpp`: senders for `PROFILE_LIST_REQ`/`PROFILE_NEW`/`PROFILE_SELECT`/`PROFILE_WEIGHT`/`PROFILE_AGE_MANUAL`; parsers for `CTRL,PROFILE_LIST` (manual comma-split, variable arity, full field validation), `CTRL,PROFILE_ACK`, `CTRL,PROFILE_RANGE` — malformed lines discarded with `COMM_LOG` per security.md. `COMM_RX_BUFFER_SIZE` bumped 512->1024 (phase 5's `WEIGHT_HISTORY`/`PROFILE_HISTORY` responses need the headroom).
  - **Implementation-revealed addition**: `CTRL,PROFILE_ACK,<seq>` (added in phase 3) is consumed here — it's how the HMI learns the seq assigned by `PROFILE_NEW`.
- [x] 4.3 Wizard state machine implemented in new `src/ui/BabyWizard.cpp`/`include/ui/BabyWizard.h`: single overlay+card widget with content cleared/rebuilt per screen (List -> New baby [name keyboard + gestation stepper] / Existing baby -> Weight [stepper + SKIP] -> Range wait -> Age in days [only if `ageKnown=0`] -> Summary -> Apply). List request has a 2s timeout with one retry, then fail-safe falls through to New baby; ACK/RANGE waits have a 3s timeout with a toast + safe fallback screen (not in the original spec text but required by the project's fail-safe convention — never hang indefinitely on a dropped response).
  - Trigger wired into `Switch_cb`'s `ui_Switch1` ON branch: switch stays visually ON (LVGL already applied checked state) but `switchTemp`/`tempSwitched` roll back to `false` so no actuation sends until `ActivateTempControlUI()` runs from the wizard's Apply. The old ON-branch body (panel highlight, arrows, chart show) was extracted verbatim into `ActivateTempControlUI(bool isAirMode)`.
- [x] 4.4 Apply wired: AIR takes the midpoint when a range exists (else stays manual, SKIP case); SKIN sets `SKIN_FIXED_SETPOINT_C` (36.5, new `main.h` constant) via `ui_Switch4`'s existing checked-branch code (re-triggered synthetically after `ActivateTempControlUI(false)`) and is refused defensively if reached without a range (should be unreachable — Summary disables Apply in that case). SKIN arrows hard-disabled: the `selectedPanel == SKIN_PANEL_SELECTED` branches were removed from both temperature arrow handlers (AIR-only now).
  - `ui_Switch4`'s checked branch also gates on a new `BabyWizard_HasUsableRange()` check (only while `tempSwitched`, so pre-selecting SKIN before activating control is unaffected) — reuses the skin-probe-toast pattern, refactored into a shared `UI_ShowToast()` helper.
- [x] 4.5 Cancel (`X`/Cancel buttons on every screen) reverts `ui_Switch1` to OFF via `ui_set_switch_state_silent` with no other state touched (nothing was changed yet, by construction — see 4.3). Critical-alarm interrupt: new `UI_IsCriticalAlarmActive()` (same ID set as motherBoard's `alarm_machine_any_critical()`) checked at the top of every `BabyWizard_Poll()` tick; on a hit, cancels exactly like the Cancel button.
- [x] 4.6 All new strings use the existing `(g_lang==LANG_ES) ? ... : (g_lang==LANG_FR) ? ... : ...` ternary pattern (ES/EN/FR), matching every other translated string in `UITask.cpp`.
- [x] 4.7 Comma key handled via input filtering (not a custom keymap): a `LV_EVENT_VALUE_CHANGED` handler on the name textarea strips any comma as it's typed — functionally equivalent to disabling the key, simpler than swapping LVGL's default keyboard map.
- [x] 4.8 `pio run -e main` on Display_HMI: SUCCESS, clean rebuild, zero warnings in any touched file.
- [ ] 4.9 Manual verification checklist on real hardware (document each item tested): new-baby activation for AIR and for SKIN; existing-baby selection; SKIP weight (AIR manual, SKIN blocked); no time sync forces manual age entry; wizard cancel leaves control OFF; critical alarm interrupts an open wizard; comma key absent from name keyboard; non-default language shows translated wizard text; a serial disconnect during the wizard is treated as a timeout, not partial data.
  - **First hardware run (HMI alone, no motherBoard) found a blocking bug — fixed, needs re-flash to re-verify.** Symptom: neither the wizard nor the Babies screen rendered anything (switch just toggled; Babies button did nothing). Root cause: `BabyWizard_Init`/`BabyHistory_Init` were called with `lv_scr_act()`, but `ui_init()` runs earlier in `UI_Task` and ends with `lv_scr_load(ui_ScreenIntro)` — so both overlays were parented to the splash screen and were gone once `ui_ScreenMain` loaded. Fixed by parenting to `ui_ScreenMain` explicitly, matching the existing convention in `create_photo_safety_popup()`. **Lesson for this checklist: a green `pio run` proves nothing about LVGL visibility** — only hardware does.
  - **Pre-existing bug of the same class, also fixed** (not introduced by this change; identical code at HEAD): `ui_SkinProbeToast` and `ui_SkinProbeStatusLabel` were likewise created on `lv_scr_act()` (= splash), so the skin-probe toast/status has never been visible in production. Fixed alongside because `UI_ShowToast()` — which every wizard/history message uses for timeout, refusal and SKIN-blocked feedback — renders through `ui_SkinProbeToast`. Worth re-verifying the original skin-probe block message too, since it should now actually appear.

## 5. Display_HMI — baby history viewer (commit scope: `hmi`)

- [x] 5.1 "Babies" top-bar button added (`ui_BabiesButton` in `ElementsCreation.cpp`, next to AlarmButton/Settings on the same top row). Plain `lv_btn` + translated label (no dedicated image asset exists; matches nothing SquareLine-generated, hand-added like the other custom widgets). Opens `BabyHistory_Open()` directly — no dependency on AIR/SKIN switches or wizard state.
- [x] 5.2 New `src/ui/BabyHistory.cpp` (+`include/ui/BabyHistory.h`), same overlay+card+poll architecture as `BabyWizard.cpp`. Active section reuses the wizard's existing `CTRL,PROFILE_LIST` parser/globals untouched (`g_pendingProfileList`/`g_profileList`), up to 3 cards with name/EG/last-weight.
- [x] 5.3 Discharge: per-card "ALTA/DISCHARGE/SORTIE" button → modal dialog with the 4 outcomes (Survived/Deceased/Transferred/Unknown) + Cancel. Confirm sends `HMI,PROFILE_DISCHARGE,<seq>,<outcome>` and waits for `CTRL,PROFILE_ACK` (toast on refusal/timeout), then refreshes both sections; Cancel sends nothing. Zero interaction with AIR/SKIN control state (no checks, no warnings — spec requirement).
- [x] 5.4 Archived section: `CTRL,PROFILE_HISTORY` parsed in `CommTask.cpp` with the same manual comma-split + per-field validation pattern as `PROFILE_LIST` (page/totalCount/n bounds, outcome 0-3, silent discard on any malformed field). Prev/next pagination driven by `totalCount` (10/page), page indicator "N / M", disabled-state buttons at the edges. Entries show name, EG, translated outcome, and discharge date (`YYYY-MM-DD` UTC from `dischargeEpoch`, "--" when 0, i.e. FIFO-evicted without explicit discharge).
- [x] 5.5 Chart screen: tapping any card (active or archived) sends `HMI,WEIGHT_HISTORY_REQ,<seq>`; `CTRL,WEIGHT_HISTORY` parsed with full validation (n≤50, per-point numeric checks). Rendered with `lv_chart` in `LV_CHART_TYPE_SCATTER` (real `dayOffset` X positions — points are not evenly spaced in time), Y range = data min/max rounded to 100 g with margin, axis ticks + "day of life" caption, baby name in the title. Empty/malformed/timeout → "no weight data" state (1 retry then empty), never partial points.
- [x] 5.6 All new strings (ES/EN/FR) via the same `g_lang` ternary pattern; the top-bar button label also re-applies on language change alongside the other main-screen labels in `UITask.cpp`'s language-apply block.
- [x] 5.7 `pio run -e main` on Display_HMI: SUCCESS, zero warnings in all touched files (`BabyHistory.cpp`, `CommTask.cpp`, `ElementsCreation.cpp`, `UITask.cpp`).
- [ ] 5.8 Manual verification checklist on real hardware (document each item tested): Babies button opens the screen from the main UI without requiring AIR/SKIN activation; active profiles show a Discharge action and archived ones do not; confirming discharge with each of the 4 outcome values sends the correct message and removes the profile from the active section; dismissing the discharge dialog changes nothing; discharging the baby currently driving active AIR or SKIN control does not stop control; paginating through more than 10 archived profiles; tapping an active baby's card opens its chart; tapping an archived baby's card opens its chart; a baby with more than 50 stored weight points renders a 50-point chart; a serial disconnect during a history/chart request is treated as no-data, not a crash or garbled display; non-default language shows translated history/discharge/chart text.

## 6. Docs / closeout

- [x] 6.1 Cross-check vs `known_issues.md` #1-#3:
  - **#1 phantom alarms**: no new alarm IDs, `CTRL,ALM`/`alarmBitmask` path untouched; wizard/history screens *consume* the bitmask (close on critical) via `UI_IsCriticalAlarmActive()`, they never publish alarm state — no new single-shot alarm messages that could be lost to EMI.
  - **#2 UART flooding**: all new `PROFILE_*`/`WEIGHT_HISTORY_*` traffic is strictly user-driven request/response (no periodic sends); the largest response (~700 chars, 50-point weight history) is one line per user tap; HMI RX line buffer raised 512→1024 accordingly; the recurring `HMI,` line actually *shrank* (13→10 fields).
  - **#3 language desync**: no new pre-formatted display text crosses the wire — `PROFILE_*` messages carry raw data; `outcome` is an integer translated HMI-side through the existing `g_lang` ternary pattern (design decision 10), so a motherBoard/HMI language mismatch cannot produce mixed-language baby screens.
- [x] 6.2 `docs/hmi.md`/`docs/communication.md` grepped for AUTO AIR/legacy baby fields: no mentions existed. Added a short "Baby-Profile Wizard & History" section to `docs/communication.md` pointing at PROTOCOL.md v2.0.0 and its breaking change so the doc doesn't go stale relative to the new message set.
- [ ] 6.3 Run `openspec archive shared-baby-profile-nte-wizard` once phases 1-5 are implemented and verified, merging the 5 capability deltas (`nte-calculation-engine`, `baby-profile-storage`, `baby-profile-protocol`, `temp-control-activation-wizard`, `baby-history-viewer`) into `openspec/specs/`.
